<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>Smart Task Manager</title>
  <link rel="stylesheet" href="/css/style.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
</head>
<body>
  <nav class="navbar">
    <div class="container">
      <div class="nav-content">
        <a href="/" class="logo">
          <span class="logo-icon">⚡</span>
          <span class="logo-text">Smart Task Manager</span>
        </a>
        <div class="nav-links">
          <a href="/" class="nav-link <%= typeof page !== 'undefined' && page === 'dashboard' ? 'active' : '' %>">Dashboard</a>
          <a href="/tasks/new" class="nav-link nav-link-primary <%= typeof page !== 'undefined' && page === 'new' ? 'active' : '' %>">
            <span class="plus-icon">+</span> New Task
          </a>
        </div>
      </div>
    </div>
  </nav>

  <main class="main-content">
    <% if (typeof body !== 'undefined') { %>
      <%- body %>
    <% } else { %>
      <div class="container">
        <p>No content to display</p>
      </div>
    <% } %>
  </main>

  <footer class="footer">
    <div class="container">
      <p>Built with AI-powered intelligence</p>
    </div>
  </footer>

  <script src="/js/main.js"></script>
</body>
</html>
